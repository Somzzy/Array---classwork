Algorithm:
Initialize an array of integers 'a'
initialize the number of left rotations the array should carry out.
Determine the length of the array n
Create a new array "rotateArray" of the same length n to store the rotated elements.
Perform the rotation. Iterate over each element of the original array a
The return the rotated array
